services:
  app:
    image: ghcr.io/school-of-company/dovi-github-app:latest
    container_name: dovi-github-app
    restart: unless-stopped
    ports:
      - '3000:3000'
    env_file:
      - .env
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'
